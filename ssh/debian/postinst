#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    # Reload SSH on Debian/Ubuntu to apply the subsystem
    if systemctl is-active --quiet ssh; then
        systemctl reload ssh || true
    fi
fi

exit 0
